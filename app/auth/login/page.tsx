export const dynamic = 'force-dynamic';

import Login from "../../dashboard/components/LoginComponent";

const LoginPage = () => {
  return (
    <div>
      <Login />
    </div>
  );
};

export default LoginPage;
